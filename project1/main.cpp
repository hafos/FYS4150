#include <fstream>
#include <string>
#include <iomanip>
#include <vector>
#include <cmath>
#include "main.hpp"

int n = 10; // grid points in square matrix
std::vector<double> a(n-1, -1); // subdiagonal
std::vector<double> b(n, 2); // diagonal
std::vector<double> c(n-1, -1); // superdiagonal
std::vector<double> g(n, 0);
for i in
std::vector<double> v(n, 0);
std::vector<double> x_array(N, 0);
double x = 0;
// filling array
for (int i=0; i <= N; i++){
    x_array[i] = x;
    x += h;
    // std::cout << x_array[i] <<"\n"; // to check array
}std::vector<double> x_array(N, 0);  // x-array
double h = 1/n
double x = 0;
// filling array
for (int i=0; i <= N; i++){
	x_array[i] = x;
	x += h;


int main(){

    general_algorithm(a, b, c, g, v);
    for (i = 0, i < n, i++){
        std::cout << v[i] << " ";
    }
    return 0
}
